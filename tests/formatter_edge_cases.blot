// Edge cases for formatter testing

// Nested expressions
nested = ((1+2)*3)/4

// Complex conditionals
cond = if a and b or c then if x then y else z else w

// Optional and rest parameters
func1=(a,b?,c?,...rest)=>a+b??0+c??0

// Nested lists and records
data = [[1,2],[3,4,{a:[5,6]}]]
config = {server:{host:"localhost",port:8080},debug:true}

// Chained operations
result = list.filter(x=>x>0).map(x=>x*2).reduce((a,b)=>a+b)

// Multiline do block with comments
compute = do {
  // Initialize values
  x = 10
  y = 20
  
  // Perform calculation
  z = x * y
  
  return z + 100
}

// Record with dynamic keys
dynamic = {
  [key1]: value1,
  ["literal-key"]: value2,
  shorthand,
  ...spread
}

// Postfix operators
fact = 5!
arr = list[0]
prop = obj.property

// Natural language operators
check = a and b or not c

// Coalesce operator
safe = value ?? default

// Power operator
squared = x^2

// Multiple statements
output x
output y = 42