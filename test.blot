// Test simple expressions
x = 5 + 3
y = x * 2

// Test conditionals
result = if x > 5 then "big" else "small"

// Test lambda functions
add = (a, b) => a + b
multiply = x => x * 2

// Test function calls
total = add(10, 20)
doubled = multiply(total)

// Test lists and records
numbers = [1, 2, 3, 4, 5]
person = { name: "Alice", age: 30 }

// Test output
print("result: {}", result)
print("doubled: {}", doubled)
